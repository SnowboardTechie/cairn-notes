# cairn-notes

Claude Code: see `@AGENTS.md` for the agent framework and conventions.

This plugin ships via Claude Code's plugin mechanism. Vault config is managed separately at `~/.claude/cairn/config.md` — run `/cairn-setup` to populate it.
